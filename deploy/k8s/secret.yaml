apiVersion: v1
kind: Secret
metadata:
  name: agentweave-proxy
  namespace: agentweave
type: Opaque
stringData:
  # Bearer token required on all incoming proxy requests.
  # Generate with: openssl rand -hex 32
  # Set in agents: Authorization: Bearer <token>
  proxy-token: "CHANGE_ME"
