apiVersion: v1
kind: Service
metadata:
  name: agentweave-proxy-nix-subagent-nodeport
  namespace: agentweave
spec:
  type: NodePort
  selector:
    app: agentweave-proxy-nix-subagent
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30402
